# MCP Server

Using the Python SDK. An exact copy-and-paste from the Github page.

I ran the following command to get the MCP inspector up and running:

```bash
pnpx @modelcontextprotocol/inspector 
```