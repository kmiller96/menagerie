name: "upstream_beta"
version: "1.0.0"

profile: "upstream_beta"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  upstream_beta:
    # Config indicated by + and applies to all files under models/example/
